/**
 * This script file contains various settings for configuring the "mentalnewsmap" 
 */

var config = {
    // how many questions before you see the result page?
    numQuestions : 5,
    
    // the url (relative or absolute) of the data file to import
    csvUrl : "data/article_data.csv",
    
    // separator and quote chars for parsing the csv:
    csvSeparator : ",",
    csvQuoteChar : "\"",
};